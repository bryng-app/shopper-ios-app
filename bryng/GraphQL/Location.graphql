mutation AddLocation($latitude: Float!, $longitude: Float!) {
    addLocation(location: {
        latitude: $latitude,
        longitude: $longitude
    }) {
        location {
            latitude
            longitude
        }
    }
}
