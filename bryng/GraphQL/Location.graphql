mutation AddLocation($latitude: Float!, $longitude: Float!) {
    addLocation(location: {
        latitude: $latitude,
        longitude: $longitude
    }) {
        geoPosition {
            latitude
            longitude
        }
        user {
            email
        }
    }
}
